import { BrowserRouter, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* Las rutas se agregarán aquí */}
      </Routes>
    </BrowserRouter>
  );
}

export default App;
